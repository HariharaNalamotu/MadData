# Web framework
flask==3.1.0
flask-cors==5.0.0
gunicorn==23.0.0

# Utilities
python-dotenv==1.0.1
Werkzeug==3.1.3

# Vector database
pymilvus==2.5.3

# PDF extraction
pypdf==5.1.0

# ML — torch is installed separately in Dockerfile (CPU-only wheel)
transformers==4.40.2
# tokenizers is pulled in by transformers; no pin needed

# LLM
openai==1.30.5
